const iconRegistry = ['cube', 'help', 'key', 'logo', 'promote', 'search', 'user', 'wallet', 'users', 'monitor', 'user-verified', 'arrow'];

type Icon = (typeof iconRegistry)[number];

export { iconRegistry, Icon };
